# 训练说明

## 问题修复

### 1. 旧的 checkpoint 不完整
之前保存的 `checkpoint_ep0.pth` 只有模型参数，没有训练状态。

**解决方案**：
- 重新开始训练（不使用 --resume）
- 或者手动创建 `checkpoint_ep0_state.pth`

### 2. 现在的checkpoint包含两个文件：
- `checkpoint_epN.pth` - 模型参数
- `checkpoint_epN_state.pth` - 训练状态（episode数、阶段、统计等）

## 正确的训练命令

### 全新训练
```bash
python train/train_sac.py
```

### 恢复训练
```bash
python train/train_sac.py --resume train/checkpoints/sac/checkpoint_ep1000.pth
```

## 输出说明

### 正常输出（每轮）：
```
Ep     1 | Stage stage1 [  0.0%] | Reward:   12.45 (avg100:  12.45) | Steps:  8 | ✓ vs basic
```

### 详细输出（每100轮）：
```
================================================================================
📊 详细统计 (Episode 100)
================================================================================
  🎯 奖励统计:
     - 最近100轮平均: 12.34
  🎮 训练参数:
     - Alpha (温度): 0.1234
  🏆 胜率统计:
     - vs basic  : 75.5% (60/100)
================================================================================
```

## 环境要求

- PyTorch with CUDA
- GPU推荐（Ada6000 / A100 / RTX 3070 Ti等）
- 训练时间：约11-14小时（70k episodes）
